# 低 token RAG

`build-index.ps1` 将规则文件和个人案例摘要生成 `index.jsonl`；`query-index.ps1` 按关键词返回少量相关片段。

日常解卦只读索引和 Top-K 摘要，不读取全部案例。完整案例仅在需要核对证据或进行反馈复盘时打开。

当前版本使用本地关键词重叠检索，不依赖外部数据库。未来可以把同一份 `index.jsonl` 接到向量数据库，字段结构保持不变。
